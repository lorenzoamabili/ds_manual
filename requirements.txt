# Core stack used by the runnable projects and the case study in this manual.
# Versions are the ones the projects were verified against; ranges keep it usable.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
statsmodels>=0.14        # forecasting (P2), survival Cox/KM (P5)
matplotlib>=3.8
seaborn>=0.13
tabulate>=0.9            # markdown tables written by the case study / projects

# Dev tooling (or: `make setup` installs these). Used by CI.
# pytest>=8.0
# ruff>=0.5

openpyxl>=3.1          # Excel reading in retail domain doc example (pd.read_excel)
mlxtend>=0.23          # association rules in retail domain doc (optional, graceful fallback)
lifelines>=0.27        # Kaplan-Meier + Cox in P5 and doc 16 examples

# Referenced in the docs (not required to run the projects); uncomment as needed:
# xgboost>=2.0
# lightgbm>=4.0
# dowhy>=0.11           # causal inference workflow (doc 09)
# econml>=0.15          # heterogeneous treatment effects (doc 09, case study)
# lifelines>=0.27       # friendlier survival API (doc 16)
# scikit-survival>=0.22 # survival ML (doc 16)
# pymc>=5.0             # Bayesian modelling (doc 20)
# networkx>=3.0         # graph analysis (doc 21)
# geopandas>=0.14       # geospatial (doc 22)
# implicit>=0.7         # collaborative filtering (doc 08)
# prophet>=1.1          # forecasting (doc 07)
# sentence-transformers # embeddings / semantic search (doc 10)
# ortools>=9.8          # optimisation (doc 12)
# mlflow>=2.10          # experiment tracking (doc 14)
